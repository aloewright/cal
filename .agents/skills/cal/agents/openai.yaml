interface:
  display_name: "Cal"
  short_description: "Repo-specific patterns and workflows for cal"
  default_prompt: "Use the cal repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true